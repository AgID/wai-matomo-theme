<!-- SiteName Injection -->
{% if currentModule is same as('CoreHome') %}
<script type="text/javascript">
    $(() => {
        window.showSiteName(piwik.siteName);
    });
</script>
{% endif %}
<!-- End SiteName Injection -->
